	var { tag, end, renderTo } = require ('../src/tagscript')
	
	var sample = 
		[ tag ('div', { id: 'main' })
		, 'Hello, '
		, tag ('em'), 'world', end ('em')
		, '!'
		, end ('div') ]
	
	renderTo (process.stdout, sample)
